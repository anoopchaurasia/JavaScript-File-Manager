FileManager is small JavaScript file which provide Java like structure for JavaScript.
It consist four basic function which are listed below -

1. fm.Import function require one argument
    It can be path divided by ".", starting from basedir.
    It can be url starting from http.
    It can be relative path with "/" and filename ending with .js.

2. fm.Base function required one argument which should be
    base class of current file class
    this function is optional
    base class can be provided in Class function as second argument.

3. fm.Class function require at most two argument and at least one argument
    first argument must be Class name and second function can be base class.

4. fm.Package function require one argument
    which must be relative directory structure divided by ".".
